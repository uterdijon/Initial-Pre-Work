/* We can use import * to import all contents of an external file. We can also specify the object that receives the imported values, like this:

import * as myObject from "my_file";

*/


import * as capitalizeStrings from "capitalize_strings";


"use strict";
