/* The equality operator == compares two values and returns true if they are equivalent and false if they are not. I made a function to test if an argument is equal to 12. */

// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);
testEqual(12);
