/* The logical or operator || returns true if either the statement on the left OR the one on the right is true */

function testLogicalOr(val) {
  // Only change code below this line

 if ( val < 10 || val > 20 ) {
    return "Outside";
    }
  // Only change code above this line
  return "Inside";
}

// Change this value to test
testLogicalOr(15);
