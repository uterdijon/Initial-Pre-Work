/* The conditional operator can be used to construct a one-line if/else statement. It takes this form:

 condition ? statement-if-true : statement-if-false

 */

function checkEqual(a, b) {
  return a == b ? true : false;
}

checkEqual(1, 2);
