/* All booleans return true or false, so we don't have to construct an if/else statement to test two values: */

function isLess(a, b) {
  return a < b;
}

// Change these values to test
isLess(10, 15);
